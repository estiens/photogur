
<% @photos.sort_by! {|photo| [photo.votes, photo.name]}.reverse! %>
<% @photos.each do |photo| %>
<p><%= link_to image_tag(photo.url, :width => "200"), "/photos/#{photo.id}" %></p>
<p>
  <%= photo.name %><br/>
  <%= photo.author %>
</p>

<% end %>